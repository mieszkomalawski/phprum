actor: ApiTester
modules:
    enabled:
        - PhpBrowser:
            url: http://localhost:8000/api
        - REST:
            url: http://localhost:8000/api
            depends: PhpBrowser
            part: Json

env:
    local:
        modules:
